#include <iostream>

using namespace std;

int main() {

    int a, b, c, d, e, f, g, h;

    cin >> a >> b >> c;
    cin.ignore(70, '\n');
    cin >> d >> e >> f;
    cin.ignore(70, '\n');
    cin >> g >> h;
    cin.ignore(70, '\n');

    return 0;
}
